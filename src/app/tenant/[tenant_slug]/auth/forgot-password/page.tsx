'use client';

interface TenantPageProps {
  params: { tenant_slug: string };
}

export default function ForgotPasswordPage({ params }: TenantPageProps) {
    return (
        <div>
            <h1></h1>
        </div>
    );
}